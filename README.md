# Seoul Sound Map

서울 자치구 위험도에 따라 색상과 사운드가 반응하는 인터랙티브 지도 MVP입니다.

## 실행 방법

```bash
npm install
npm run dev
```

브라우저에서 `http://localhost:3000`으로 접속하세요.

## 안내

- Audio는 하단 시트의 "소리 켜기" 버튼을 클릭한 뒤에만 시작됩니다.
- `data/districts.ts`의 mock 위험도는 추후 실제 데이터로 교체할 수 있습니다.
